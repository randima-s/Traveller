import {CAROUSELDATA} from "../shared/carouseldata";
import * as ActionTypes from "./ActionTypes";

export const CarouselData=(state=CAROUSELDATA,action)=>{
    switch(action.type){
        default:
            return state;
    }
}